# Safe Node Fixture

This repository exists only to verify bounded static analysis. The package script deliberately fails if executed, so a passing analyzer test proves that repository commands were not invoked.
